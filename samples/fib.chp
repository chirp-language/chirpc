func int fib(int: n)
{
	int: a = 0;
	int: b = 1;
	while n != 0
	{
		const int: tmp = b;
		n = n - 1;
		b = a + b;
		a = tmp;
	}
	ret a;
}

import "io"
namespace io
{
	func none print(char const ptr: msg, int: param);
}

entry
{
	io.print("Result of fib(5): ", fib(5));
}
